# 受講者用サイト　講師用の運用手順

## フォルダの中身

| 場所 | 中身 | GitHubに上げるか |
|---|---|---|
| `site/` | 受講者が見るページ一式（これがそのままGitHub Pagesになる） | **上げる** |
| `tools/content.py` | 要点まとめ・演習・参考サイトの文章（**解答の解説も平文で入っている**） | 上げない |
| `tools/build.py` | presentpackのノートからページを作り直すスクリプト | 上げない |
| `tools/answer_keys.json` | 解答を開く鍵（**秘密**） | **絶対に上げない** |
| `tools/講師パネル.bat` | 講義中に解答を開く操作パネル | 上げない |

公開URL（予定）：https://umeharatakahito.github.io/prompt-engineering-1day/

## 講義中の操作

1. オープニングで、URL（とQRコード）をZoomチャットとスライドで案内する
2. プロンプトを使う場面では「プロンプト集の **No.○○** です」と言う
   - 演習は「演習ページの **演習1** です」
3. 解答・解説を見せたいタイミングで、`tools/講師パネル.bat` をダブルクリック
   - 番号を入力してEnter → その演習が「公開中」になる
   - 受講者の画面は1分以内に自動で開く（受講者が🔒の行をクリックすると、その場で確認する）
4. 研修が終わったら、講師パネルで **R**（すべて閉じる）

※一度開いた受講者の端末では、閉じたあとも開いたままになります（その端末に記憶されるため）。

## 台本（presentpack）を直したとき

`tools` フォルダで次を実行すると、ページが作り直されます。

```bash
python build.py
```

そのあと `site/` の変更をGitHubに上げます（コミットしてpush）。

- プロンプト集は、ノートの［チャットに送る］の枠から自動で作られます
- 演習・要点まとめ・参考サイトの文章は `tools/content.py` を直します
- 事務連絡など、サイトに載せないものは `content.py` の `SKIP_TEXT` に追加します

## 鍵を変えたいとき（別の研修回で、前回の受講者に先に見られたくない場合）

`tools/answer_keys.json` を削除して `python build.py` を実行すると、新しい鍵で作り直されます。
そのあと `site/` をGitHubに上げます。

## 仕組み（参考）

- 解答はページの中に**暗号化して**入っています。ソースを見ても読めません
- 講師パネルが、GitHub上の `release.json` に「開けてよい演習の鍵」を書き込みます
- 受講者のページは、1分ごとに `release.json` を見に行き、鍵があれば自動で開きます
- 受講者のページは、GitHubの確認窓口に1時間あたり60回までしか問い合わせできません。同じ会社から多人数が同じ回線で見ていると上限に当たることがありますが、その場合はサイト側の `release.json` を見に行くので、1〜2分遅れて開きます
